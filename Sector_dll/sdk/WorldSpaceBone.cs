using Sector_dll.cheat;

namespace Sector_dll.sdk
{
    class WorldSpaceBone
    {

        //public static object GetType(object bone)
        //{
        //    if (SignatureManager.WorldSpaceBone_type == null)
        //        return 0;
        //    return SignatureManager.WorldSpaceBone_type.GetValue(bone); //untested
        //}

        //public static object GetHead(object bone)
        //{
        //    if (SignatureManager.WorldSpaceBone_head == null)
        //        return null;
        //    return SignatureManager.WorldSpaceBone_head.GetValue(bone);
        //}

        //public static object GetTail(object bone)
        //{
        //    if (SignatureManager.WorldSpaceBone_tail == null)
        //        return null;
        //    return SignatureManager.WorldSpaceBone_tail.GetValue(bone);
        //}

        //public static float GetRadius(object bone)
        //{
        //    if (SignatureManager.WorldSpaceBone_radius == null)
        //        return 0;
        //    return (float)SignatureManager.WorldSpaceBone_radius.GetValue(bone);
        //}

        //public static int GetID(object bone)
        //{
        //    if (SignatureManager.WorldSpaceBone_ID == null)
        //        return -1;
        //    return (int)SignatureManager.WorldSpaceBone_ID.GetValue(bone);
        //}

        //public static string GetName(object bone)
        //{
        //    if (SignatureManager.WorldSpaceBone_name == null)
        //        return "";
        //    return (string)SignatureManager.WorldSpaceBone_name.GetValue(bone);
        //}

    }
}
